import 'package:flutter/services.dart';

class AppColors {
  static const primary = Color(0xFF329D9C);
  static const secondary = Color(0xFF56C596);
  static const accent = Color(0xFF7BE495);
  static const background = Color(0xFFF7FBFC);
  static const darkBlue = Color(0xFF205072);
  static const textPrimary = Color(0xFF205072);
  static const textSecondary = Color(0xFF5C6B73);
}
